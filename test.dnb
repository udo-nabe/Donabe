func makeCounter(start) {
    var count = start;

    let increment = func() {
        count += 1;
        return count;
    };

    return increment;
}

let a = makeCounter(0);
let b = makeCounter(100);

print(a());
print(b());
print(a());
print(b());